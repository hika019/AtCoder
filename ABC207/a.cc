#include <iostream>
#include <cmath>
#include <algorithm>
#include <vector>
#include <cassert>
#include <numeric>
#include <string>
#include <map>
using namespace std;

int main(){

  int a, b, c, d, i, ans;

  std::cin >> a >> b >> c >> d;

  if(b>=c) c=-1;
  else{
    while()
  }

  return 0;
}
