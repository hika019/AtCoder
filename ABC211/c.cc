#include <iostream>
#include <cmath>
#include <algorithm>
#include <vector>
#include <cassert>
#include <numeric>
#include <string>
#include <map>
using namespace std;

int main(){
  std::string s;
  int i, j, k, l, m, n, o, p;
  map<std::string, int> dic;

  std::cin >> s;

  for(i=0; i<s.size(); i++){
    if(dic[s[i]] == 0){
      
    }
  }


  return 0;
}
